(module info (lib "infotab.ss" "setup")
  (define name "Graph Search")
  (define blurb
    (list "A simple rule language and graph search procedure for "
          "so state space searches."))
  (define primary-file "graphsearch.ss")
  (define doc.txt "doc.txt")
  (define categories '(misc)))